# OpenAI API Key for DALL-E image generation
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...your-api-key-here...

# Alternative: Stability AI for Stable Diffusion (cheaper alternative)
# STABILITY_API_KEY=sk-...your-api-key-here...

# FTP Server Configuration (for admin interface)
# Used to upload deutsch-words.json directly to production server
# Leave empty to disable FTP upload (saves locally only)
FTP_HOST=ftp.example.com
FTP_USER=your_ftp_username
FTP_PASSWORD=your_ftp_password
FTP_SECURE=false
FTP_REMOTE_PATH=/data/deutsch-words.json

# Usage:
# 1. Copy this file to .env
# 2. Fill in your FTP credentials
# 3. Run: npm start
# 4. Admin interface will upload to FTP when saving
